## R CMD check results

0 errors | 0 warnings | 1 note

* This is a revised submission of a new package.
* In response to the CRAN incoming pre-test note, the examples for
  `persuasio4ytz()` and `persuasio4ytz2lpr()` were shortened by reducing
  the bootstrap replications shown in the examples from `nboot = 1000` to
  `nboot = 100`. The function defaults and package behavior were not changed.

Checks were run on:

* macOS (aarch64-apple-darwin20) using R 4.5.2
* Windows (x86_64-w64-mingw32, R-devel) using R Under Development (2026-06-01 r90092)

No errors or warnings were reported.
